<table class="table">
  <thead>
   <th>ID</th>
   <th>Branch</th>
   <th>Description</th>
   <th></th>
  </thead>
  <tbody>
  <tr>
   <td><%= @driver_drop.id %></td>
   <td><%= @driver_drop.branch %></td>
   <td><%= @driver_drop.description %></td>
   <td>
    <div class="btn-group">
     <button class="btn btn-danger">Work On Drop Here</button>
     <button class="btn btn-danger dropdown-toggle" data-toggle="dropdown">
     <span class="caret"></span>
     </button>
       <ul class="dropdown-menu">
       <li>
        <a href= <%= new_package_path %> >Package Diver Drop</a>
       </li>
       <li>
        <a href="#">Create Drop Mail</a>
       </li>
       <li>
        <a href= <%= new_driver_drop_log_path(@driver_drop)%> >Upload Test Logs</a>
       </li>
       <li>
       <li class="divider"></li>
       <li>
        <li>
        <a href= <%= edit_driver_drop_path(@driver_drop) %> >Edit Drop Details</a>
       </li>
       </ul>
    </div>
   </td>
   </tr>
 </tbody>
</table>
<p>


<table class="table">
<thead>
  <th>x86 Failures</th>
</thead>
<tbody>
  <tr>
   <% @x86log.each do |x| %> 
    <tr><td> id:<%= x.id %>  <%= x.name %></td></tr>
    <% end %>
  </tr>
</tbody>
</table>

<table class="table">
<thead>
    <th>x64 Failures</th>
</thead>
<tbody>
  <tr>
   <% @x64log.each do |x| %> 
   <tr> <td> id:<%= x.id %>  <%= x.name %></td></tr>
    <% end %>   
  </tr>
</tbody>
</table>
<p>
<p>
<p>
<%= @log_count %> Log files have been uploaded and processed for this driver drop.
<p>
<%= @log_nill_count %> Total number of log files with "unknown" platform. <br>
